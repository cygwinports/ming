--- origsrc/ming-0.4.2/perl_ext/Makefile.PL	2008-07-13 15:14:52.000000000 -0500
+++ src/ming-0.4.2/perl_ext/Makefile.PL	2009-04-13 22:16:48.807625000 -0500
@@ -146,7 +146,7 @@ WriteMakefile(
     clean   => {
         FILES   => "@do_clean",
     },
-    'MYEXTLIB'     => $MING_TOP_BUILDDIR . '/src/.libs/libming.a',
+    'MYEXTLIB'     => $MING_TOP_BUILDDIR . '/src/.libs/libming.dll.a',
     'OBJECT'       => "$XS_OBJECTS",
     'LDFROM'       => "$LD_OBJECTS",
     'INC'          => "-I$MING_TOP_SRCDIR/src -I$BUILDDIR -I$MING_TOP_BUILDDIR/src -I$SRCDIR",
@@ -164,7 +164,7 @@ sub MY::install {
 
 sub ming_is_compiled
 {
-	my $libming = $MING_TOP_BUILDDIR."/src/.libs/libming.a";
+	my $libming = $MING_TOP_BUILDDIR."/src/.libs/libming.dll.a";
 
 	if ( -e $libming )
 	{
--- origsrc/ming-0.4.2/perl_ext/Makefile.am	2008-07-13 08:46:56.000000000 -0500
+++ src/ming-0.4.2/perl_ext/Makefile.am	2009-04-13 22:18:08.463875000 -0500
@@ -32,13 +32,13 @@ EXTRA_DIST = SWF/Action.pm SWF/BinaryDat
 #abs_builddir=`cd $(TMP_DIR); pwd`
 
 all: Makefile.perl
-	$(MAKE) -f Makefile.perl
+	$(MAKE) -f Makefile.perl all manifypods
 
 install: Makefile.perl
 	$(MAKE) -f Makefile.perl install PREFIX=$(prefix)
 
 Makefile.perl: Makefile.PL
-	srcdir=$(srcdir) builddir=$(abs_builddir) $(PERL) $< PREFIX=$(prefix) FIRST_MAKEFILE=Makefile.perl
+	srcdir=$(srcdir) builddir=$(abs_builddir) $(PERL) $< PREFIX=$(prefix) FIRST_MAKEFILE=Makefile.perl INSTALLDIRS=vendor
 
 clean-local: Makefile.perl
 	$(MAKE) -f Makefile.perl realclean
